const { Pool } = require('pg'); const url = process.env.DATABASE_URL || 'postgresql://postgres:Tejaswini@03@localhost:5432/sentinews'; const pool = new Pool({ connectionString: url }); (async () => { try { const res = await pool.query('CREATE EXTENSION IF NOT EXISTS vector;'); console.log('extension result', res.command); } catch (e) { console.error('ERROR', e.message); process.exit(1); } finally { await pool.end(); }})();
